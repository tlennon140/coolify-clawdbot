version: "3.9"

services:
  dev-shell:
    build: .
    image: custom-clawdbot:latest
    container_name: dev-clawdbot
    tty: true
    stdin_open: true
    volumes:
      - dev-clawdbot-data:/app
    environment:
      - DEBIAN_FRONTEND=noninteractive

volumes:
  dev-clawdbot-data:
